import { WrapperCard } from "@/components/Card";

const SecondHandSolving = () => {
  return (
    <WrapperCard className="flex flex-col items-center gap-y-[24px] grow w-full text-[24px] leading-[32px] font-medium">
      waiting for the first hand to resolve the result &#40; will automatically
      redirect to result &#41;
    </WrapperCard>
  );
};

export default SecondHandSolving;
